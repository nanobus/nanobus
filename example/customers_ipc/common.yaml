# filters:
#   http:
#     - type: jwt
#       with:
#         rsaPublicKeyFile: ../../public.pem

codecs:
  avro:
    type: confluentavro
    with:
      schemaRegistryURLs:
        - http://localhost:8081

errors:
  timeout:
    type: Timeout
    code: internal
    title: Operation timed out
    message: The operation did not complete in the time allowed.

  permission_denied:
    type: PermissionDenied
    code: permission_denied
    title: Permission denied
    message: You are not authorized to perform this operation.
