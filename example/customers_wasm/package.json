{
  "name": "customers_wasm",
  "version": "0.0.1",
  "description": "Customers Service as WebAssembly",
  "author": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "asc assembly/index.ts -b build/customers_wasm.wasm --use abort=assembly/index/abort --optimize"
  },
  "license": "ISC",
  "dependencies": {
    "@wapc/as-guest": "^v0.2.1",
    "@wapc/as-msgpack": "^0.1.13"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.8"
  }
}
